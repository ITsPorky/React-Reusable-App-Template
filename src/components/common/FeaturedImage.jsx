import React from 'react';

const FeaturedImage = ({children, id, Style, className = "", image}) => {
    let styles = {

    }
    return (
        <section>

        </section>
    );
}
export default FeaturedImage;